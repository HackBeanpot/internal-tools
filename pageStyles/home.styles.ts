import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

const StyledTypographyBody1 = styled(Typography)({
  margin: "16px 0",
});

export { StyledTypographyBody1 };
